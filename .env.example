# DigPhy demo mode — no environment variables required.
# The app runs entirely on in-memory mock data (lib/data/mock-store.ts).
# Supabase has been removed; the following vars are no longer needed:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
